# E7 计划 —— 响应 Mladen 第二轮评审(W1 标定 / 消散修复 / 动态平衡 / 跨速度迁移)

状态:方案待确认,未动代码。对应他邮件的四个实质要求 + 一个理想目标。

## E7a — Wasserstein 标定(方法升级,基础设施)

- 目标函数换为 **1-Wasserstein**:对每个时刻 t,W1(t) = ∫₀^{20km} |N_sim(x,t) − N_data(x,t)| dx,其中 N(x,t)=∫₀ˣρ dx'(累计密度,不归一化——质量差自然计入);J = mean_t W1(t),窗口同现有(t∈[100,1000] s)。
- 用 W1 重新拟合 A=1、A=10 的 (κ_c, κ_r)(lf 形式);与 RMSE 版并排报告场图/剖面/指标。
- 预期:激波位置和尾流密度的权衡改善(RMSE 会为了压平错位惩罚而"摊薄"队列,W1 不会)。
- 新指标 W1 同时加入评估表(三个模型 M1/M2/M3 都算),RMSE 保留。ω 指标按 Mladen 的观察降级为参考项(下游密度已验证有效通过量)。

## E7b — 消散修复(两个正交机制,消融实验;他明说 "needs to be fixed")

**(i) 释放斜坡(先做,+1 参数,物理直白)**:t > t_fast 后 u_s(t) 不再瞬时跳回 v_f,而是以有限加速度 a_rel 线性上升(τ_ramp = (v_f−u_ξ)/a_rel ≈ 5–13 s @ 2.6–1 m/s²,可标定)。期间 Δv > 0 ⇒ 释放持续、s 滞后 f 前冲 ⇒ 密集区被拉开,产生类消散。物理对应:被释放车辆的加速过程。
**(ii) 光滑凹 FD(若 (i) 不够;.tex 已预告)**:三角形换光滑正则化(临界点附近 smooth-min,平滑半宽 δ 为参数),次临界高密度区获得 dλ/dρ < 0 ⇒ 真正的消散扇。作为 FD 选项实现(默认仍三角形,CFL 不变),A=1 案例上验证释放后的扩散形态。
验收标准:A=1 释放后 t∈[800,1000] 的密集区宽度增长率非零、与数据的消散形态在剖面图上可比。

## E7c — 尾流密度 + A=10 动态平衡(参数/结构探索,回应他的"ideal outcome")

在 W1 目标下重扫 (κ_c, κ_r);再试两个"最小复杂化"变体(各 +1 参数,消融矩阵评估):
- **(a) 捕获局域化**:捕获主体 ℓ = a + γ·s,γ∈[0,1] 标定。γ 小 ⇒ 抑制自催化、屏障贴紧 CAV(修图 3 的"太厚");γ=1 退回现 lf;γ=0 退回 .tex 严格 af。
- **(b) stuck 类专属通量函数**(他的原话建议):s 类拥堵支独立参数 w_s ≥ w(更陡)或 P_s ≤ P——stuck 车队能装得更密 ⇒ 修尾流密度 44 vs 58 的缺口。
- **A=10 动态平衡的判据**(他描绘的图景,量化验收):CAV 后 ≤500 m 内存在 s>0 的稳态薄层、周转时间 1/μ ≈ 20–50 s(与 E1 实测一致)、其上游出现"慢但不堵死"带(速度介于 u_ξ 与 v_f、密度高于来流)。达不到就如实报告"结构极限"——他明确接受这个结论选项。

## E7d — 跨速度迁移(防过拟合,他的最后一问)

数据核查:A∈{1,10} 只有 u∈{15,20};u=10 仅 A=3 有。因此两层测试:
1. **A=1 / A=10**:u15 拟合 → **u20 零调参预测**,对照"u20 自拟合"的上限(ceiling);
2. **A=3(更强)**:补 u15_q2500 场拟合 → 横扫 u∈{10,12,18,20,24}×q2500 **全部零调参预测**,画 W1/RMSE vs u 的 **speed-transfer 曲线**(论文稳健性一节主图)。u_ξ 在模型中是物理输入(类速度),κ 不随 u 变——曲线平坦即证明"nice and robust, not overfitted"。

## 执行顺序与产物

E7a(W1 基础设施+重拟合)→ E7d 基线(现有 κ 先跑迁移,留前后对比)→ E7b(i) → E7c 消融 → 必要时 E7b(ii) → 全部图表在最终配置下重出。
产物:`e7_wasserstein.py`(W1 目标+重拟合)、`solver.py` 增量(释放斜坡、γ、w_s/P_s、光滑 FD 选项,全部默认关闭、逐位向后兼容+回归测试)、`e7_transfer.py`(迁移曲线)、`E7_results.md`、更新 README/推送。
